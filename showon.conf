# จะถูกเขียนใหม่ตอน Install
# ตัวอย่างค่าเริ่มต้น (Install จะเติมค่าจริงให้อัตโนมัติ)

VERSION="V.1.0.5"
WWW_DIR="/var/www/html/server"
LIMIT=2000
DEBUG_LOG="/var/log/showon-debug.log"

# 3x-ui
PANEL_BASE=""     # เช่น https://ip:port/path
XUI_USER=""
XUI_PASS=""

# network interface (ถ้าเว้นว่าง สคริปต์จะพยายามหาให้อัตโนมัติ)
NET_IFACE=""
